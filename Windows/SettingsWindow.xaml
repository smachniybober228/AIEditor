<Window x:Class="AIEditor.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIEditor.Windows"
        mc:Ignorable="d"
        Title="SettingsWindow" Height="110" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Выбранная модель:" FontSize="15" Margin="5" HorizontalAlignment="Right"/>
            <ComboBox Grid.Column="1" Margin="5" ItemsSource="{Binding Models}" SelectedIndex="0"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Сохранить" FontSize="15" Margin="5"/>
            <Button Grid.Column="1" Content="Вернуться" FontSize="15" Margin="5"/>
        </Grid>
    </Grid>
</Window>
